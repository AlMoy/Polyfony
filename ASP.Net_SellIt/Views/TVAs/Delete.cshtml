@model EntityASP.Entity.TVA

@{
    ViewBag.Title = "Supprimer un TVA";
}

    <div>
        <h3>Voulez vous supprimer ce taux de TVA?</h3>
        <hr />

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                @Html.ActionLink("Retour à la liste", "Index", "TVAs", null, new { @class = "btn btn-primary" })
                <input type="submit" value="Supprimer" class="btn btn-primary" />
            </div>
        }

        <h4 class="mt-3">Détaill du TVA</h4>
        <hr />
        <div class="d-flex flex-column">
            <table class="table table-striped table-bordered table-hover">
                <thead class="thead-dark">
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.EndDate)</th>
                        <th>@Html.DisplayNameFor(model => model.Rate)</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>
                            @Html.DisplayFor(model => model.EndDate)
                            @Html.DisplayFor(model => model.Rate)
                        </td>

                    </tr>

                </tbody>

            </table>
        </div>
    </div>
