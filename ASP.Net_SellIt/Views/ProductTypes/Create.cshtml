@model EntityASP.Entity.ProductType

@{
    ViewBag.Title = "Création un type de produit";
}

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">

        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        <div class="input-group mt-auto">
            <div class="input-group-prepend">
                @Html.LabelFor(m => m.Name, new { @class = "input-group-text" })
            </div>
            @Html.EditorFor(m => m.Name, new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
        </div>

        <div class="input-group mt-3">
            <div class="input-group-prepend">
                @Html.LabelFor(m => m.Price, new { @class = "input-group-text" })
            </div>
            @Html.EditorFor(m => m.Price, new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.Price, "", new { @class = "text-danger" })
        </div>
               
        <div class="col-md-offset-2 mt-3">
            @Html.ActionLink("Retour à la liste", "Index", "ProductTypes", null, htmlAttributes: new { @class = "btn btn-primary" })
            <input type="submit" value="Créer" class="btn btn-primary" />

        </div>
                  
    </div>
}


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
